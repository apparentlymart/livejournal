<?_code # -*-bml-*-
{
    use strict;
    use vars qw(%GET);

    my $control_strip;
    if (defined $GET{'user'}) {
        $control_strip = LJ::control_strip( user => $GET{'user'} );
    }

    sleep 1 if $LJ::IS_DEV_SERVER;

    return LJ::js_dumper($control_strip);
}
_code?>
