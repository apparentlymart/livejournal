<?_code
{
    return "<?badinput?>" unless LJ::text_in(\%GET);
    my $wav;

    eval { $wav = LJ::Captcha::get_audio_data($GET{capid}, $GET{anum}); };
    if ($wav) {
        BML::set_content_type("audio/x-wav");
        return BML::noparse($wav);
    } else {
        return "Error: $@";
    }
           
}
_code?>
