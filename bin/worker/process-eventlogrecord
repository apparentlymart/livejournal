#!/usr/bin/perl
use strict;
use lib "$ENV{LJHOME}/cgi-bin";
LJ::NewWorker::TheSchwart::EventLogRecord->start();

package LJ::NewWorker::TheSchwart::EventLogRecord;
use strict;
use lib "$ENV{LJHOME}/cgi-bin";
use base 'LJ::NewWorker::TheSchwartz';
require 'ljlib.pl';
require 'ljprotocol.pl';
use LJ::EventLogRecord;

sub capabilities { map { [ $_ => '' ] } LJ::EventLogRecord->schwartz_capabilities() }

1;
